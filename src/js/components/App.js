import React from 'react';
import Form from './InputForm';

const App = () => {
    return (
        <div>
            <h1>Hello, React!</h1>
            <p>This React app is now injected into your HTML template.</p>
            <Form />
        </div>
    );
};

export default App;
